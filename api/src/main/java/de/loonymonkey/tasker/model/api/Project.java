package de.loonymonkey.tasker.model.api;

/**
 * a project. Well, something which will represent a project when it's grown up.
 */
public interface Project extends TaskList {
    String getTitle();
}
